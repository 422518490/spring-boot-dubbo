/*
package com.provider.config;

import com.alibaba.dubbo.config.ServiceConfig;
import com.provider.service.impl.UserServiceImpl;
import com.service.UserService;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

*/
/**
 * @author liaoyubo
 * @version 1.0
 * @date 2019/6/21
 * @description
 *//*

@Configuration
public class UserServiceBeanConfig {

    @Bean
    public ServiceConfig<UserServiceImpl> userServiceServiceConfig(UserServiceImpl userService){
        ServiceConfig<UserServiceImpl> config = new ServiceConfig<UserServiceImpl>();
        config.setInterface(UserService.class);
        config.setRef(userService);
        config.setGroup("user");
        return config;
    }
}
*/
